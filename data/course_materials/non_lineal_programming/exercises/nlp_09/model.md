# P9 — Modelo

## Solución

1. Calcular gradiente de $f(x, y)$:
    - $\nabla f(x,y) = (12x - 10, 6y - 1)$
2. Calcular matriz Hessiana:
$$
H =
\begin{pmatrix}
12& 0 \\
0& 6
\end{pmatrix}
$$
    - $h_1 = 12 > 0$
    - $h_2 = 72 > 0$
    - $\therefore$ La matriz $H$ es definida positiva $\forall (x, y)$ $\rightarrow$ es convexa en todo $x, y$
    - Respuesta $\rightarrow$ alternativa **3** 