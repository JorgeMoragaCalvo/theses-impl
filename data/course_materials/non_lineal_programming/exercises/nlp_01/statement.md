# P1 — Optimización con restricciones

Para el problema:

$ min f(x, y) = (x - 4)^2 + (x - 2y)^2 $  
$ s.a. x^2 - y = 0 $

Se piensa que $ (-1, 1) $ y $ (1, 1) $ son soluciones óptimas.

## Lo que se pide

# P2 — Optimización con restricciones
Para el problema:

$ min f(x, y) = (x - 2)^2 + (y - 1)^2 $  
$ s.a. -y + x^2 \le 0 $  
$ s.a. x + y - 2 \le 0 $

Se piensa que $ (1, 1) $ es solución óptima.

## Lo que se pide

# P3 — Optimización con restricciones
Se busca construir una caja rectangular sin tapa desde una pieza $ 12 m^2 $ de carton.

## Lo que se pide
- Encontrar el volumen máximo de la caja.